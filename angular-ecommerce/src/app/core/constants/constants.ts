export const 
    API_URL = "http://localhost:3000/";

export const ValidationMessage: Record<string, string> = {
    REQUIRED: "This field is required",
    EMAIL: "Enter Valid Email",
    MIN_CHAR: "Min 6 char needed"
}